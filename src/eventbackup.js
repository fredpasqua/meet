{
  /* <p className="eventDateTime">{`${event.start.dateTime} +  " (" + ${event.start.timeZone} + ")"`}</p>
                    <p className="etag-location1">{`${event.etag} + " (" + ${event.location} + ")"`}</p>
                    <p className="about-event2">About event:</p>
                    <a href="www.googel.com2">See details on Google Calendar</a>
                    <p className="event-description2">{event.description}</p>
                    <button className="details-toggle-button1"></button> */
}
